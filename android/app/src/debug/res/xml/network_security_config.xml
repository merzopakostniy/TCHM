<?xml version="1.0" encoding="utf-8"?>
<!--
  Только для отладочной сборки: эмуляторы Firebase работают по обычному
  HTTP, а Android с 9-й версии его запрещает. Разрешение выдано ровно двум
  адресам — хосту разработчика. В релизной сборке этого файла нет.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- так Android-эмулятор видит хост-машину -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
